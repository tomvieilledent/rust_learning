/**
 * run - Calculate and display the sum of two numbers
 *
 * Description: Adds two integers and prints the result
 */
pub fn run() {
    let a = 10;
    let b = 4;
    let sum = a + b;

    println!("The sum is {}", sum);
}
