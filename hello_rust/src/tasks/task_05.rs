/**
 * run - Calculate the average of three numbers
 *
 * Description: Computes and displays the average of three
 * integer values
 */
pub fn run() {
    let x = 10;
    let y = 20;
    let z = 30;
    let average = (x + y + z) / 3;

    println!("The average is {}", average);
}
